.env
__pycache__/
*.pyc
.venv/
venv/
*.db
*.sqlite
data/cache/
.DS_Store
.streamlit/secrets.toml
.indmoney_tokens.json
.indmoney_tokens.client.json

# Personal financial reports. never commit
Indmoney-*.xlsx
consolidated_tax_report*.xlsx
*TaxPnlReport*.xlsx
*TransactionsReport*.xlsx

# Bake-off outputs (regenerated by analyzer.bakeoff, don't track)
data/bakeoff/

# Blueprint 13 fine-tune JSONL export. Repo is public (confirmed live via
# gh repo view --json isPrivate) - upload these to a Kaggle private
# dataset instead, never commit real prediction/trading data here.
data/finetune/

# Local scratch
probe.txt

# Claude Code local machine/session settings - never tracked (confirmed:
# zero git history), formalizing that here. settings.local.json in
# particular carries local tool permissions, not project config.
.claude/

# Session/chat-context handoff notes - local planning artifacts, never
# tracked (confirmed: zero git history since project start). Repo is
# public; these are working notes, not project documentation.
context/
