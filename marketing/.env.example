# Google Apps Script Web App URL that appends rows to your waitlist sheet.
# Setup: create a Google Sheet, open Extensions > Apps Script, paste the script
# in docs/waitlist-apps-script.gs, deploy it as a Web App ("Execute as: me",
# "Who has access: Anyone"), copy the generated /exec URL, and paste it below.
WAITLIST_WEBHOOK_URL=
